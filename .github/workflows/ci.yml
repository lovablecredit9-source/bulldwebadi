name: CI

# Disabled: project is tested manually as requested.
# Kept as a placeholder so no automatic Actions run is triggered.
on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI disabled"
